{
  "name": "@nexaboard/shared",
  "version": "0.1.0",
  "private": true,
  "description": "nexaBoard shared utilities and types",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
